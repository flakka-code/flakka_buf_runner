name: flakka_buf_runner
description: A run-process helper for using the buf CLI
version: 0.1.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  build:
  build_config:
  build_runner:
  build_runner_core:
  build_test:
  glob:
  package_config:
  path:
#  path_provider:
  scratch_space:

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^5.1.0
